#include <stdio.h>

int main() {
	int Min, Max;
	printf("Nhap so tu nhien Max:");
	scanf("%d", &Max);
	printf("Nhap so tu nhien Min:");
	scanf("%d", &Min);

	float tong = 0, soBien = 0, trungBinh = 0;

	return 0;
}
}
